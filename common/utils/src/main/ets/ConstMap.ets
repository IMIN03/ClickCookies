import {ConstStr} from './ConstStr'

export const ConstMap = new Map<string, string>([
  [ConstStr.WORKSHOP_STORAGE_URL, '/workshopArchive.json'],
  [ConstStr.SHOP_STORAGE_URL, '/shopArchive.json'],
  [ConstStr.ACCIDENT_STORAGE_URL, '/accidentArchive.json'],
  [ConstStr.SKILLTREE_STORAGE_URL, '/skillTreeArchive.json'],
]);
